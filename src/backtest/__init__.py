"""The backtest harness.

Build it early, run it late. Before roughly six months of recorded screening
days exist, every number this package can produce is noise that looks like
signal, and the harness refuses rather than obliges.
"""
